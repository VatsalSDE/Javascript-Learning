"Use Strict"; // Treat all code as a newer version of javascript 
// alert(3 + 3) we are using node js not browser

console.log(3+3); // Code readibility should be high
console.log("Vatsal"); 

let name="Vatsal"
let age=19
let isLoggedIn=true

name="Deepak" // here we can change the variable value because we have declared it as a let datatype 
console.log(name);

console.log(isLoggedIn);